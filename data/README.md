This directory stores local datasets and generated samples.

Recommended layout:

- `raw/`: immutable source data
- `processed/`: cropped or pooled learning inputs
- `generated/`: diffusion-model outputs used for augmentation

Large files are excluded from git by default.
